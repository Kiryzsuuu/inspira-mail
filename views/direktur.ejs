<%- include('./partials/header') %>

<div class="page-header">
  <div class="page-title-group">
    <div class="page-breadcrumb"><a href="/inbox">Dashboard</a> › Ringkasan Organisasi</div>
    <h1 class="page-title">Ringkasan Organisasi</h1>
    <p class="page-subtitle">Pantau sirkulasi surat dinas di seluruh organisasi</p>
  </div>
</div>

<div class="stats-bar" style="margin-bottom:24px;">
  <div class="stat-card">
    <div class="stat-icon blue">
      <svg width="18" height="18" viewBox="0 0 20 20" fill="none"><circle cx="8" cy="7" r="3" stroke="currentColor" stroke-width="1.5"/><path d="M2 18c0-3.314 2.686-6 6-6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/><circle cx="15" cy="12" r="3" stroke="currentColor" stroke-width="1.5"/></svg>
    </div>
    <div><div class="stat-label">Total Pengguna</div><div class="stat-value"><%= totalUsers %></div></div>
  </div>
  <div class="stat-card">
    <div class="stat-icon green">
      <svg width="18" height="18" viewBox="0 0 20 20" fill="none"><path d="M18 2L9 11M18 2l-6 16-3-7-7-3 16-6z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
    </div>
    <div><div class="stat-label">Surat Terkirim</div><div class="stat-value"><%= sentMails.length %></div></div>
  </div>
  <div class="stat-card">
    <div class="stat-icon amber">
      <svg width="18" height="18" viewBox="0 0 20 20" fill="none"><path d="M4 4h12M4 8h8M4 12h5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
    </div>
    <div><div class="stat-label">Draf Aktif</div><div class="stat-value"><%= draftCount %></div></div>
  </div>
</div>

<div class="mail-list-wrap">
  <div class="mail-list-header">
    <span class="mail-list-title">Surat Terbaru Seluruh Organisasi</span>
    <div class="filter-tabs">
      <button class="filter-tab active" onclick="filterMails(this,'all')">Semua</button>
      <button class="filter-tab" onclick="filterMails(this,'penting')">Penting</button>
    </div>
  </div>

  <% if (sentMails.length === 0) { %>
  <div class="empty-state">
    <svg width="48" height="48" viewBox="0 0 48 48" fill="none"><path d="M8 12h32M8 20h32M8 28h20" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
    <p>Belum ada surat yang beredar.</p>
  </div>
  <% } %>

  <% sentMails.forEach(function(m, i) {
    const initials = m.fromName.split(' ').slice(0,2).map(w => w && w[0] ? w[0] : '').join('').toUpperCase() || 'SU';
    const tagClass = { Penting: 'tag-penting', Urgent: 'tag-urgent', Info: 'tag-info', Normal: 'tag-normal' }[m.tag] || 'tag-normal';
    const avColors = ['av-green','av-teal','av-blue','av-amber','av-purple'];
    const avColor = avColors[i % avColors.length];
  %>
  <div class="mail-item" data-tag="<%= (m.tag || 'normal').toLowerCase() %>">
    <div class="mail-avatar <%= avColor %>"><%= initials %></div>
    <div class="mail-body">
      <div class="mail-from">Dari: <%= m.fromName %> &rarr; <%= m.toNames %></div>
      <div class="mail-subject"><%= m.subject %></div>
      <% if (m.berkas) { %><div class="mail-berkas"><%= m.berkas %></div><% } %>
    </div>
    <div class="mail-meta">
      <span class="mail-date"><%= m.date %></span>
      <span class="mail-tag <%= tagClass %>"><%= m.tag || 'Normal' %></span>
    </div>
  </div>
  <% }); %>
</div>

<%- include('./partials/footer') %>
